# MDS Fixtures

Small, checked-in custom-MDS fixtures for the reduced-round scout harness.

Each JSON file records a concrete 16x16 matrix over the KoalaBear field and the
SHA-256 of the canonical JSON matrix encoding used by `custom_mds_scout`'s
admissibility bridge.
